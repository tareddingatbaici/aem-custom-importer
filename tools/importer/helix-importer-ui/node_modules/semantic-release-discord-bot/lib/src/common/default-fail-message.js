"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.defaultFailMessage = void 0;
function defaultFailMessage(packageName, errors) {
    var message = "Something went wrong while trying to publish the new version of `" + packageName + "`!";
    for (var _i = 0, errors_1 = errors; _i < errors_1.length; _i++) {
        var error = errors_1[_i];
        message += "\n\n" + error.name + "\n" + error.message + "\n";
        message += "```" + error.stack + "```";
    }
    return {
        message: message,
        format: 'markdown'
    };
}
exports.defaultFailMessage = defaultFailMessage;
