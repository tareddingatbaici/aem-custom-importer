import * as semantic from 'semantic-release';
import { TGBotMessage } from '../interfaces/message';
export declare function defaultSuccessMessage(packageName: string, nextRelease: semantic.NextRelease): TGBotMessage;
