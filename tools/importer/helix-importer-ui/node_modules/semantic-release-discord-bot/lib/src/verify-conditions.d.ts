import { TGBotConfig } from './interfaces/config';
import * as semantic from 'semantic-release';
export declare function verifyConditions(config: TGBotConfig, { logger, env }: semantic.Context): void;
